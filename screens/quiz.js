import React from "react";
import { StyleSheet, Text, View } from 'react-native';

const Quiz = () => {
  return (
     <View>
      <Text>Quiz page</Text>
    </View>
  );
};

export default Quiz;

const styles = StyleSheet.create({});