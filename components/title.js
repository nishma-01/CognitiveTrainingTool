import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Title = () => {
  return (
     <View>
      <Text>Cognitive Training Tool</Text>
    </View>
  );
};

export default Title;

const styles = StyleSheet.create({});
